[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    real_sleep: opts out of the sleep-collapsing fixture; asserts real elapsed time
    slow: takes tens of seconds; deselect with -m "not slow"
